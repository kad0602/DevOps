services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: go-gin-api
    ports:
      - "8080:8080"
    environment:
      - GIN_MODE=release
    restart: unless-stopped
